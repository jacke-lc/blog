---
import PostMeta from '@/components/blog/PostMeta.astro';

interface Props {
  post: {
    slug: string;
    data: {
      title: string;
      description: string;
      category: string;
      pubDate: Date;
      tags?: string[];
      featured?: boolean;
    };
  };
}

const { post } = Astro.props;
---

<article class="group panel p-6 transition hover:border-cyan-400/20 hover:bg-white/[0.06]">
  <div class="flex items-start justify-between gap-4">
    <div>
      {post.data.featured && <p class="mb-3 text-xs uppercase tracking-[0.28em] text-cyan-300/80">Featured</p>}
      <h3 class="text-2xl font-semibold tracking-tight text-white">
        <a href={`/blog/${post.slug}`} class="hover:text-zinc-300">{post.data.title}</a>
      </h3>
    </div>
  </div>
  <p class="relative mt-4 text-sm leading-7 text-zinc-400">{post.data.description}</p>
  <div class="relative mt-6">
    <PostMeta date={post.data.pubDate} category={post.data.category} tags={post.data.tags} />
  </div>
</article>
